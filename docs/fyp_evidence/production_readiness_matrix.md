# Production Readiness Matrix

| Subsystem | Development/demo status | Production requirement | Current production status | Risk | Required deployment action |
| --- | --- | --- | --- | --- | --- |
| Auth/session security | JWT auth, RBAC, and object authorization are active in local smoke runs. Dev mode still allows an ephemeral JWT secret when `AEGIS_JWT_SECRET` is unset. | Fixed `AEGIS_JWT_SECRET`, HTTPS, secure cookie settings, controlled bootstrap admin credentials, and deployment-time auth validation. | Not configured locally for production. | High | Set production secrets, enforce HTTPS, verify cookie security, rotate bootstrap credentials, and run production auth validation. |
| PostgreSQL | Development runtime works with JSONL or in-memory persistence. | `POSTGRES_DSN`, schema bootstrap/migrations, health checks, backups, and fail-fast validation. | **Phase 53 PASSED** — PostgreSQL provisioned via Docker Compose, 18 tables bootstrapped and verified, `POSTGRES_DSN` configured in gitignored env file, `validate_runtime --profile production` 119/119. | Low | Production DSN must point to a managed/durable Postgres instance (not Docker default credentials) for live deployment. |
| Redis | Optional in development. | Durable Redis configuration for required caches/queues if production architecture depends on it. | **Phase 53 PASSED** — Redis provisioned via Docker Compose, PING/SET/GET/TTL validated, `REDIS_URL` configured. | Low | Redis should be replaced with a managed instance (e.g. ElastiCache) and TLS URL for live deployment. |
| Model registry | File-backed registry is readable and active models resolve locally. | Controlled registry backend, promotion/rollback governance, audited writes, and deployment-safe pointer management. | Partially configured; local file registry only. | Medium | Finalize production registry/write policy and validate promotion/rollback operations in deployment. |
| Object authorization | Camera/case/uploaded-video authorization paths are active and API smoke preserves 401/403 behavior. | Full scoped access review against production identities, cameras, cases, uploads, and audit expectations. | Code paths are present; production policy has not been independently hardened in this local run. | Medium | Run scoped authorization regression against production identity data and security review coverage. |
| Evidence storage | Local filesystem and metadata storage are working for demo flows. | Managed artifact storage, retention enforcement, backup/restore validation, and restricted download controls. | Not configured locally for managed production storage. | High | Configure durable evidence storage, retention jobs, integrity monitoring, and access controls. |
| Drone simulation | Development/demo runtime supports simulated drone status and optional live simulator checks. | If retained in deployment, simulator isolation and explicit simulated-only policy must remain intact. | Local-only simulated runtime; not a production target as-is. | Medium | Keep simulator isolated from production claims and document it as simulated-only infrastructure. |
| GIS map provider | `local_mock` provider works without a paid key. | Production map/tile provider or approved offline provider, token management, and availability validation. | Not configured beyond local mock. | Medium | Configure the intended production map provider and validate token/key handling. |
| OpenAI/LLM | Local stub fallback is allowed in development; LLM-safe case workflow passes with operator-review caveats. | `OPENAI_API_KEY`, approved model policy, cost controls, audit logging, and no unsafe autonomous report wording. | **Phase 53 PASSED** — `OPENAI_API_KEY` installed in gitignored env file; live Responses API call verified; `reasoning` parameter fix applied for non-reasoning models; `validate_runtime --profile production` 119/119. | Low | Key must remain in a secrets manager (not env file) for live deployment; rotate before deploy. |
| OSINT enrichment | Analyst-provided-only mode is active; network fetching is intentionally disabled. | Approved ingestion workflow, audit coverage, storage policy, and analyst controls. | Partially ready; still limited to analyst-provided data. | Medium | Preserve analyst-provided-only mode unless a reviewed enrichment pipeline is introduced. |
| Streaming RTSP/WebRTC/HLS | Streaming config, HLS, replay, and ffmpeg checks pass in development. | Production camera onboarding, TURN/WebRTC operations, monitoring, retention, and external network validation. | Not validated against production camera infrastructure locally. | High | Validate real stream sources, TURN/networking, storage quotas, and replay operations in deployment. |
| Uploaded video | Local uploaded-video workflow smoke completes with sample data. | Durable storage, upload quotas, malware/media policy controls, and production retention/export governance. | Partially ready; local storage only. | Medium | Configure production storage and rerun uploaded-video smoke under deployment settings. |
| Analytics | Analytics overview and health endpoints are reachable locally. | Durable data pipeline, export controls, retention, and deployment observability. | Partially ready. | Medium | Validate analytics sources and exports against production data stores. |
| Model governance | Registry/governance APIs are reachable. Development governance is currently degraded by missing production-style anomaly live-eval evidence. | Full promotion evidence, known limitations, evaluation reports, and deployment governance policy enforcement. | Not fully ready for production promotion gates. | High | Produce required live-eval/governance evidence and rerun production governance validation. |
| Investigation | Camera graph and path-reconstruction flows work with demo GIS profiles and honest `insufficient_data` handling. | Real geolocated observations, retention, and production-scale authorization coverage. | Partially ready; local demo data only. | Medium | Validate investigation workflows against real geolocated evidence in deployment. |
| Drone mission | Mission planner routes and simulated mission data are available locally. | If retained for deployment, simulated-only policy, operator start controls, and live simulator validation must remain explicit. | Partial and simulator-dependent. | Medium | Revalidate against Blocks runtime when used and keep simulated-only controls mandatory. |
| Drone fusion | Deterministic fusion smoke and health endpoint pass with safe wording. | Real geo-calibrated multi-source observations, review workflow operations, and deployment storage policy. | Partial and simulator/data dependent. | Medium | Validate live simulator fusion when used and verify operator review flows with real-like data. |
