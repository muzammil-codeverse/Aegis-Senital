# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — Sentinel AI training orchestration config (local runner only)
#
# This file is consumed by ml/training/train.py (the local training wrapper).
# It is NOT a YOLO dataset config.
#
# CANONICAL YOLO DATASET CONFIG (used by all scripts/ training scripts):
#   → configs/weapon.yaml
#
# Do NOT pass this file as the --data argument to ultralytics YOLO.
# ─────────────────────────────────────────────────────────────────────────────

dataset_path: ""          # path to YOLO data.yaml (configs/weapon.yaml)
epochs: 50
batch_size: 16
img_size: 640
model: yolov8n.pt
output_path: ""
experiment_name: weapon_detect
